language: python
branches:
  only:
  - master
python:
- '3.6'
node_js: 6
install:
- pip install -U pip
- pip install -r requirements.txt
script:
- make html
after_success:
deploy:
  provider: pages
  skip_cleanup: true
  github_token: "$token"
  local_dir: build/html
  on:
    branch: master